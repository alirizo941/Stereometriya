<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.stereometriya1">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stereometriya1"
        tools:targetApi="31">

        <activity
            android:name=".DetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Stereometriya1" />

        <!-- ✅ Yangi LearnDetailActivity qo‘shildi -->
        <activity
            android:name=".LearnDetailActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Stereometriya1">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity 
            android:name=".DefinitionTestActivity"
            android:exported="false"
            android:theme="@style/Theme.Stereometriya1"
            android:parentActivityName=".MainActivity" />
            
        <activity 
            android:name=".FormulaTestActivity"
            android:exported="false"
            android:theme="@style/Theme.Stereometriya1"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".TestResultActivity"
            android:exported="false"
            android:theme="@style/Theme.Stereometriya1"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>
